import styles from './Auth.module.scss';

function LoginPage() {
    return (
        <div className={styles.auth_wrapper}>
            LoginPage
        </div>
    );
}

export default LoginPage;